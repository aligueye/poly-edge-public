FROM python:3.12-slim
RUN useradd -m -u 1000 polyedge
WORKDIR /app
COPY python/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY python/ .
USER polyedge
ENTRYPOINT ["python", "sidecar/server.py"]
